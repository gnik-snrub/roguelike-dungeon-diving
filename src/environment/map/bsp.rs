/*
use crate::environment::map::create_room;
use crate::environment::*;

use rand::*;

pub fn bsp(
    rooms: &mut Vec<Rect>,
    mut map: &mut Map,
    colors: &[Color; 7],
    player: &mut Object
) {
    let div_depth = 3;
    let mut divisions = 0;
    let limit_x = 41;
    let limit_y = 78;
    let mut bound_x = 0;
    let mut bound_y = 0;

    while divisions < div_depth {
        let variance = rand::thread_rng().gen_range(1, 5);
        if rand::random() {

        } else {

        }
        divisions += 1;
    }
}
*/
